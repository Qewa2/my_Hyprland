#include <iostream>

int main(){
    // type conversion is used to change the data type of a variable

    double x = (int)3.14; // 3.14 is converted to an int first and is then assinged to x as a double

    std::cout << x << '\n';

    return 0;
}